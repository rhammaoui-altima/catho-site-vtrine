<div class="col-md-4 mb-5">
    <div class="card h-100">
        <div class="card-body">
            <h2 class="card-title">{{ service.title|title }}</h2>
            <p class="card-text">{{ service.content|truncate(150) }}</p>
        </div>
        <div class="card-footer">
            <a href="#" class="btn btn-primary btn-sm">More Info</a>
        </div>
    </div>
</div>